name: Global Test Summary (All Batches)

on:
  # Auto-trigger after BOTH batches complete
  workflow_run:
    workflows:
      - "Test All Packages (Batch 1) on Arm64"
      - "Test All Packages (Batch 2) on Arm64"
  # Manual trigger for ad-hoc summaries
  workflow_dispatch:

jobs:
  global-summary:
    name: Generate Global Summary
    runs-on: ubuntu-24.04-arm
    # Run even if batches fail
    if: always()

    steps:
      - name: Check if other batches are still running
        id: check-batches
        uses: actions/github-script@v7
        with:
          script: |
            // Get the workflow run that triggered this
            const triggeringRun = context.payload.workflow_run;
            const triggeringWorkflowName = triggeringRun.name;
            
            console.log(`Triggered by: ${triggeringWorkflowName}`);
            
            // Define the batches we care about
            const batch1Name = "Test All Packages (Batch 1) on Arm64";
            const batch2Name = "Test All Packages (Batch 2) on Arm64";
            
            // Determine which one is the "other" batch
            let otherBatchName = "";
            if (triggeringWorkflowName === batch1Name) {
              otherBatchName = batch2Name;
            } else if (triggeringWorkflowName === batch2Name) {
              otherBatchName = batch1Name;
            } else {
              console.log("Triggered manually or by unknown workflow. Proceeding.");
              return true; // Proceed if manual trigger
            }
            
            console.log(`Checking status of other batch: ${otherBatchName}`);
            
            // Find the latest run of the OTHER batch
            const runs = await github.rest.actions.listWorkflowRuns({
              owner: context.repo.owner,
              repo: context.repo.repo,
              workflow_id: otherBatchName === batch1Name ? "test-all-packages-batch1.yml" : "test-all-packages-batch2.yml",
              branch: triggeringRun.head_branch,
              status: "in_progress"
            });
            
            if (runs.data.total_count > 0) {
              console.log(`⚠️ ${otherBatchName} is still running (${runs.data.total_count} active runs).`);
              console.log("Skipping summary generation until the last batch completes.");
              core.setOutput("skip", "true");
            } else {
              console.log(`✅ ${otherBatchName} is NOT running. We are the last one!`);
              core.setOutput("skip", "false");
            }

      - name: Exit if other batches are running
        if: steps.check-batches.outputs.skip == 'true'
        run: |
          echo "Other batch is still running. Exiting to avoid duplicate summary."
          exit 0

      - name: Checkout repository
        if: steps.check-batches.outputs.skip != 'true'
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          # Use the branch that triggered the workflow_run
          ref: ${{ github.event.workflow_run.head_branch || github.ref_name }}

      - name: Pull latest test results from repo
        run: |
          echo "Pulling latest commits with test results..."
          git pull origin "${{ github.event.workflow_run.head_branch || github.ref_name }}" || true

      - name: Build test-results structure from committed JSON
        run: |
          mkdir -p test-results
          
          # Recreate artifact-like structure for compatibility
          for f in data/test-results/*.json; do
            [ -f "$f" ] || continue
            base=$(basename "$f" .json)
            mkdir -p "test-results/${base}-test-results"
            cp "$f" "test-results/${base}-test-results/${base}.json"
          done
          
          echo "✅ Prepared $(find test-results -name '*.json' | wc -l) test result files"

      - name: Generate global summary
        run: |
          echo "# 📊 Global Package Test Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "_Last updated: $(date -u +'%Y-%m-%d %H:%M:%S UTC')_" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          TOTAL=0
          PASS=0
          FAIL=0

          # Process all JSON files
          for result_dir in test-results/*/; do
            for json_file in "$result_dir"*.json; do
              [ -f "$json_file" ] || continue

              TOTAL=$((TOTAL + 1))
              
              NAME=$(jq -r '.package.name' "$json_file")
              VERSION=$(jq -r '.package.version' "$json_file")
              STATUS=$(jq -r '.run.status' "$json_file")
              PASSED=$(jq -r '.tests.passed' "$json_file")
              FAILED=$(jq -r '.tests.failed' "$json_file")

              if [ "$STATUS" = "success" ]; then
                PASS=$((PASS + 1))
                echo "✅ **$NAME** (v$VERSION): $PASSED tests passed" >> $GITHUB_STEP_SUMMARY
              else
                FAIL=$((FAIL + 1))
                echo "❌ **$NAME** (v$VERSION): $FAILED tests failed" >> $GITHUB_STEP_SUMMARY
              fi
            done
          done

          echo "" >> $GITHUB_STEP_SUMMARY
          echo "## Overall Summary" >> $GITHUB_STEP_SUMMARY
          echo "- **Total Packages:** $TOTAL" >> $GITHUB_STEP_SUMMARY
          echo "- **Passed:** $PASS ✅" >> $GITHUB_STEP_SUMMARY
          echo "- **Failed:** $FAIL ❌" >> $GITHUB_STEP_SUMMARY
          
          if [ $TOTAL -eq 0 ]; then
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "⚠️ **Warning:** No test results found. Batches may still be running." >> $GITHUB_STEP_SUMMARY
          fi