name: Test Luigi on Arm64

on:
  workflow_dispatch:
  workflow_call:
    outputs:
      contract_version: { value: "${{ jobs.test-luigi.outputs.contract_version }}" }
      package_slug: { value: "${{ jobs.test-luigi.outputs.package_slug }}" }
      package_name: { value: "${{ jobs.test-luigi.outputs.package_name }}" }
      package_version: { value: "${{ jobs.test-luigi.outputs.package_version }}" }
      run_status: { value: "${{ jobs.test-luigi.outputs.run_status }}" }
      badge_status: { value: "${{ jobs.test-luigi.outputs.badge_status }}" }
      core_failed: { value: "${{ jobs.test-luigi.outputs.core_failed }}" }
      tests_passed: { value: "${{ jobs.test-luigi.outputs.tests_passed }}" }
      tests_failed: { value: "${{ jobs.test-luigi.outputs.tests_failed }}" }
      tests_skipped: { value: "${{ jobs.test-luigi.outputs.tests_skipped }}" }
      duration_seconds: { value: "${{ jobs.test-luigi.outputs.duration_seconds }}" }
      regression_status: { value: "${{ jobs.test-luigi.outputs.regression_status }}" }
      regression_decision: { value: "${{ jobs.test-luigi.outputs.regression_decision }}" }
      regression_result: { value: "${{ jobs.test-luigi.outputs.regression_result }}" }
      regression_comparison: { value: "${{ jobs.test-luigi.outputs.regression_comparison }}" }
      regression_current_version: { value: "${{ jobs.test-luigi.outputs.regression_current_version }}" }
      regression_latest_version: { value: "${{ jobs.test-luigi.outputs.regression_latest_version }}" }
      regression_next_installed_version: { value: "${{ jobs.test-luigi.outputs.regression_next_installed_version }}" }
      regression_policy: { value: "${{ jobs.test-luigi.outputs.regression_policy }}" }
      run_id: { value: "${{ jobs.test-luigi.outputs.run_id }}" }
      run_attempt: { value: "${{ jobs.test-luigi.outputs.run_attempt }}" }
      job_name: { value: "${{ jobs.test-luigi.outputs.job_name }}" }
      dashboard_link: { value: "${{ jobs.test-luigi.outputs.dashboard_link }}" }
      timestamp: { value: "${{ jobs.test-luigi.outputs.timestamp }}" }

jobs:
  test-luigi:
    permissions:
      contents: read
    runs-on: ubuntu-24.04-arm
    outputs:
      contract_version: "2.0"
      package_slug: luigi
      package_name: Luigi
      package_version: ${{ steps.smoke.outputs.version || 'unknown' }}
      run_status: ${{ steps.summary.outputs.overall_status || 'failure' }}
      badge_status: ${{ steps.summary.outputs.badge_status || 'failing' }}
      core_failed: ${{ steps.summary.outputs.core_failed || '0' }}
      tests_passed: ${{ steps.summary.outputs.passed || '0' }}
      tests_failed: ${{ steps.summary.outputs.failed || '0' }}
      tests_skipped: ${{ steps.summary.outputs.skipped || '0' }}
      duration_seconds: ${{ steps.summary.outputs.duration || '0' }}
      regression_status: ${{ steps.smoke.outputs.test6_status || 'skipped' }}
      regression_decision: ${{ steps.smoke.outputs.regression_decision || 'not_configured' }}
      regression_result: ${{ steps.smoke.outputs.regression_result || '' }}
      regression_comparison: ${{ steps.smoke.outputs.regression_details || 'Regression comparison summary unavailable.' }}
      regression_current_version: ${{ steps.smoke.outputs.regression_current_version || steps.smoke.outputs.version || 'unknown' }}
      regression_latest_version: ${{ steps.smoke.outputs.regression_latest_version || 'unknown' }}
      regression_next_installed_version: ${{ steps.smoke.outputs.regression_next_installed_version || 'not_installed' }}
      regression_policy: applicable
      run_id: ${{ github.run_id }}
      run_attempt: ${{ github.run_attempt }}
      job_name: ${{ github.job }}
      dashboard_link: /opensource_packages/luigi
      timestamp: ${{ steps.metadata.outputs.timestamp }}
    steps:
      - uses: actions/checkout@v4
      - id: metadata
        run: echo "timestamp=$(date -u +%Y-%m-%dT%H:%M:%SZ)" >> "$GITHUB_OUTPUT"
      - id: smoke
        uses: ./.github/actions/run-missing-package-smoke
        with:
          package_slug: luigi
          package_name: Luigi
          kind: luigi-source
          baseline_version: 1.0.0
          candidate_version: 2.29.0
          source_repo: luigi-project/luigi
          source_tag: v1.0.0
          candidate_source_tag: v2.29.0
          expected_paths: core/package.json,client/package.json,docs,package.json
      - id: summary
        if: always()
        uses: ./.github/actions/emit-package-result
        with:
          package_slug: luigi
          package_name: Luigi
          version: ${{ steps.smoke.outputs.version || 'unknown' }}
          install_status: ${{ steps.smoke.outputs.install_status || 'failed' }}
          test1_label: "Download Luigi baseline source"
          test2_label: "Verify expected source layout"
          test3_label: "Check core package metadata"
          test4_label: "Architecture Verification"
          test5_label: "Search source/docs functional markers"
          test1_status: ${{ steps.smoke.outputs.test1_status || 'skipped' }}
          test2_status: ${{ steps.smoke.outputs.test2_status || 'skipped' }}
          test3_status: ${{ steps.smoke.outputs.test3_status || 'skipped' }}
          test4_status: ${{ steps.smoke.outputs.test4_status || 'skipped' }}
          test5_status: ${{ steps.smoke.outputs.test5_status || 'skipped' }}
          test6_status: ${{ steps.smoke.outputs.test6_status || 'skipped' }}
          test1_duration: ${{ steps.smoke.outputs.test1_duration || 0 }}
          test2_duration: ${{ steps.smoke.outputs.test2_duration || 0 }}
          test3_duration: ${{ steps.smoke.outputs.test3_duration || 0 }}
          test4_duration: ${{ steps.smoke.outputs.test4_duration || 0 }}
          test5_duration: ${{ steps.smoke.outputs.test5_duration || 0 }}
          test6_duration: ${{ steps.smoke.outputs.test6_duration || 0 }}
          regression_decision: ${{ steps.smoke.outputs.regression_decision || 'not_configured' }}
          regression_current_version: ${{ steps.smoke.outputs.regression_current_version || steps.smoke.outputs.version || 'unknown' }}
          regression_latest_version: ${{ steps.smoke.outputs.regression_latest_version || 'unknown' }}
          regression_next_installed_version: ${{ steps.smoke.outputs.regression_next_installed_version || 'not_installed' }}
          regression_result: ${{ steps.smoke.outputs.regression_result || 'Regression result unavailable.' }}
          regression_details: ${{ steps.smoke.outputs.regression_details || 'Regression comparison summary unavailable.' }}
      - if: always()
        run: |
          if [ "${{ steps.summary.outputs.should_fail }}" = "1" ]; then
            exit 1
          fi
