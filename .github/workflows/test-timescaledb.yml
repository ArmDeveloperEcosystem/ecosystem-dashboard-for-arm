name: Test TimescaleDB on Arm64

on:
  workflow_dispatch:
  workflow_call:
    outputs:
      contract_version:
        description: Output contract version
        value: ${{ jobs.test-timescaledb.outputs.contract_version }}
      package_slug:
        description: Canonical package slug
        value: ${{ jobs.test-timescaledb.outputs.package_slug }}
      package_name:
        description: Display package name
        value: ${{ jobs.test-timescaledb.outputs.package_name }}
      package_version:
        description: Baseline version tested in this run
        value: ${{ jobs.test-timescaledb.outputs.package_version }}
      run_status:
        description: Overall package run status
        value: ${{ jobs.test-timescaledb.outputs.run_status }}
      badge_status:
        description: Badge status derived from core baseline checks
        value: ${{ jobs.test-timescaledb.outputs.badge_status }}
      core_failed:
        description: Number of failed baseline core checks (Tests 1-5)
        value: ${{ jobs.test-timescaledb.outputs.core_failed }}
      tests_passed:
        description: Count of passed tests
        value: ${{ jobs.test-timescaledb.outputs.tests_passed }}
      tests_failed:
        description: Count of failed tests
        value: ${{ jobs.test-timescaledb.outputs.tests_failed }}
      tests_skipped:
        description: Count of skipped tests
        value: ${{ jobs.test-timescaledb.outputs.tests_skipped }}
      duration_seconds:
        description: Total duration across tests
        value: ${{ jobs.test-timescaledb.outputs.duration_seconds }}
      regression_status:
        description: Regression test status
        value: ${{ jobs.test-timescaledb.outputs.regression_status }}
      regression_decision:
        description: Regression decision label
        value: ${{ jobs.test-timescaledb.outputs.regression_decision }}
      regression_result:
        description: Human readable regression result
        value: ${{ jobs.test-timescaledb.outputs.regression_result }}
      regression_comparison:
        description: Regression comparison note
        value: ${{ jobs.test-timescaledb.outputs.regression_comparison }}
      regression_current_version:
        description: Baseline version used for regression comparison
        value: ${{ jobs.test-timescaledb.outputs.regression_current_version }}
      regression_latest_version:
        description: Next candidate version selected for regression validation
        value: ${{ jobs.test-timescaledb.outputs.regression_latest_version }}
      regression_next_installed_version:
        description: Candidate version reported by installed next artifact
        value: ${{ jobs.test-timescaledb.outputs.regression_next_installed_version }}
      run_id:
        description: GitHub run id
        value: ${{ jobs.test-timescaledb.outputs.run_id }}
      run_attempt:
        description: GitHub run attempt
        value: ${{ jobs.test-timescaledb.outputs.run_attempt }}
      job_name:
        description: GitHub job identifier
        value: ${{ jobs.test-timescaledb.outputs.job_name }}
      dashboard_link:
        description: Dashboard route for this package
        value: ${{ jobs.test-timescaledb.outputs.dashboard_link }}
      timestamp:
        description: UTC timestamp for this package run
        value: ${{ jobs.test-timescaledb.outputs.timestamp }}
jobs:
  test-timescaledb:
    runs-on: ubuntu-24.04-arm
    env:
      PG_MAJOR: "17"
      TIMESCALEDB_VERSION: "2.25.1"
      TIMESCALEDB_NEXT_VERSION: "2.25.2"

    outputs:
      contract_version: "2.0"
      package_slug: ${{ steps.metadata.outputs.package_slug }}
      package_name: 'TimescaleDB'
      package_version: ${{ steps.version.outputs.version || 'unknown' }}
      run_status: ${{ steps.summary.outputs.overall_status || 'failure' }}
      badge_status: ${{ steps.summary.outputs.badge_status || 'failing' }}
      core_failed: ${{ steps.summary.outputs.core_failed || '0' }}
      tests_passed: ${{ steps.summary.outputs.passed || '0' }}
      tests_failed: ${{ steps.summary.outputs.failed || '0' }}
      tests_skipped: ${{ steps.summary.outputs.skipped || '0' }}
      duration_seconds: ${{ steps.summary.outputs.duration || '0' }}
      regression_status: ${{ steps.test6.outputs.status || 'skipped' }}
      regression_decision: ${{ steps.test6.outputs.decision || 'not_configured' }}
      regression_result: ${{ steps.test6.outputs.regression_result || '' }}
      regression_comparison: ${{ steps.test6.outputs.comparison || 'Regression comparison summary unavailable.' }}
      regression_current_version: ${{ steps.test6.outputs.current_version || steps.version.outputs.version || 'unknown' }}
      regression_latest_version: ${{ steps.test6.outputs.latest_version || 'unknown' }}
      regression_next_installed_version: ${{ steps.test6.outputs.next_installed_version || 'not_installed' }}
      run_id: ${{ github.run_id }}
      run_attempt: ${{ github.run_attempt }}
      job_name: ${{ github.job }}
      dashboard_link: ${{ steps.metadata.outputs.dashboard_link }}
      timestamp: ${{ steps.metadata.outputs.timestamp }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set test metadata
        id: metadata
        run: |
          echo "timestamp=$(date -u +%Y-%m-%dT%H:%M:%SZ)" >> "$GITHUB_OUTPUT"
          echo "package_slug=timescaledb" >> "$GITHUB_OUTPUT"
          echo "dashboard_link=/opensource_packages/timescaledb" >> "$GITHUB_OUTPUT"
          echo "current_version=${TIMESCALEDB_VERSION}" >> "$GITHUB_OUTPUT"
      - name: Install TimescaleDB
        id: install
        run: |
          echo "Installing TimescaleDB..."
          export DEBIAN_FRONTEND=noninteractive

          sudo rm -f /etc/apt/sources.list.d/pgdg.list /etc/apt/sources.list.d/timescaledb.list
          sudo rm -f /usr/share/keyrings/postgresql.gpg /usr/share/keyrings/timescaledb.gpg

          sudo apt-get update
          sudo apt-get install -y curl gnupg ca-certificates lsb-release postgresql-common

          curl -fsSL https://www.postgresql.org/media/keys/ACCC4CF8.asc | sudo gpg --dearmor -o /usr/share/keyrings/postgresql.gpg
          echo "deb [signed-by=/usr/share/keyrings/postgresql.gpg] https://apt.postgresql.org/pub/repos/apt $(lsb_release -cs)-pgdg main" | sudo tee /etc/apt/sources.list.d/pgdg.list >/dev/null

          curl -fsSL https://packagecloud.io/timescale/timescaledb/gpgkey | sudo gpg --dearmor -o /usr/share/keyrings/timescaledb.gpg
          echo "deb [signed-by=/usr/share/keyrings/timescaledb.gpg] https://packagecloud.io/timescale/timescaledb/ubuntu/ $(lsb_release -cs) main" | sudo tee /etc/apt/sources.list.d/timescaledb.list >/dev/null

          sudo apt-get update

          BASELINE_PACKAGE=$(apt-cache madison "postgresql-${PG_MAJOR}-timescaledb" | awk -v want="${TIMESCALEDB_VERSION}" '$3 ~ "^"want { print $3; exit }')
          if [ -z "${BASELINE_PACKAGE}" ]; then
            echo "install_status=failed" >> "$GITHUB_OUTPUT"
            echo "Baseline package ${TIMESCALEDB_VERSION} is not available for PostgreSQL ${PG_MAJOR} on this Arm64 apt repository."
            exit 1
          fi

          sudo apt-get install -y "postgresql-${PG_MAJOR}" "postgresql-client-${PG_MAJOR}" "postgresql-${PG_MAJOR}-timescaledb=${BASELINE_PACKAGE}"

          if [ -f "/usr/share/postgresql/${PG_MAJOR}/extension/timescaledb.control" ] && [ -f "/usr/lib/postgresql/${PG_MAJOR}/lib/timescaledb.so" ]; then
            echo "baseline_package=${BASELINE_PACKAGE}" >> "$GITHUB_OUTPUT"
            echo "install_status=success" >> "$GITHUB_OUTPUT"
          else
            echo "install_status=failed" >> "$GITHUB_OUTPUT"
            exit 1
          fi

      - name: Get TimescaleDB version
        id: version
        run: |
          set -euo pipefail
          VERSION=$(dpkg-query -W -f='${Version}\n' "postgresql-${PG_MAJOR}-timescaledb" | grep -Eo '[0-9]+\.[0-9]+\.[0-9]+' | head -1 || echo "unknown")
          LATEST="${TIMESCALEDB_NEXT_VERSION}"
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          echo "latest=$LATEST" >> "$GITHUB_OUTPUT"
          echo "Detected TimescaleDB version: $VERSION"
          echo "Detected latest TimescaleDB version: $LATEST"

      - name: Test 1 - Check extension assets exist
        id: test1
        continue-on-error: true
        run: |
          START_TIME=$(date +%s)
          if [ -f "/usr/share/postgresql/${PG_MAJOR}/extension/timescaledb.control" ] && [ -f "/usr/lib/postgresql/${PG_MAJOR}/lib/timescaledb.so" ]; then
            echo "status=passed" >> "$GITHUB_OUTPUT"
          else
            echo "status=failed" >> "$GITHUB_OUTPUT"
            exit 1
          fi
          END_TIME=$(date +%s)
          echo "duration=$((END_TIME - START_TIME))" >> "$GITHUB_OUTPUT"

      - name: Test 2 - Check package version
        id: test2
        continue-on-error: true
        run: |
          START_TIME=$(date +%s)
          if dpkg-query -W -f='${Version}\n' "postgresql-${PG_MAJOR}-timescaledb" | grep -q "${TIMESCALEDB_VERSION}"; then
            echo "status=passed" >> "$GITHUB_OUTPUT"
          else
            dpkg-query -W -f='${Version}\n' "postgresql-${PG_MAJOR}-timescaledb" || true
            echo "status=failed" >> "$GITHUB_OUTPUT"
            exit 1
          fi
          END_TIME=$(date +%s)
          echo "duration=$((END_TIME - START_TIME))" >> "$GITHUB_OUTPUT"

      - name: Test 3 - Check control metadata
        id: test3
        continue-on-error: true
        run: |
          START_TIME=$(date +%s)
          CONTROL_FILE="/usr/share/postgresql/${PG_MAJOR}/extension/timescaledb.control"
          if grep -q "default_version = '${TIMESCALEDB_VERSION}'" "$CONTROL_FILE" && psql --help >/dev/null 2>&1; then
            echo "status=passed" >> "$GITHUB_OUTPUT"
          else
            cat "$CONTROL_FILE" || true
            echo "status=failed" >> "$GITHUB_OUTPUT"
            exit 1
          fi
          END_TIME=$(date +%s)
          echo "duration=$((END_TIME - START_TIME))" >> "$GITHUB_OUTPUT"

      - name: Test 4 - Architecture Verification
        id: test4
        continue-on-error: true
        run: |
          START_TIME=$(date +%s)
          ARCH=$(uname -m)
          if [ "$ARCH" = "aarch64" ]; then
            echo "status=passed" >> "$GITHUB_OUTPUT"
          else
            echo "status=failed" >> "$GITHUB_OUTPUT"
            exit 1
          fi
          END_TIME=$(date +%s)
          echo "duration=$((END_TIME - START_TIME))" >> "$GITHUB_OUTPUT"

      - name: Test 5 - Functional Validation
        id: test5
        continue-on-error: true
        run: |
          set -euo pipefail
          START_TIME=$(date +%s)
          TEST_PORT=$(python3 - <<'PY'
          import socket
          with socket.socket() as s:
              s.bind(("", 0))
              print(s.getsockname()[1])
          PY
          )

          cleanup() {
            sudo pg_dropcluster --stop "${PG_MAJOR}" tsdbtest >/dev/null 2>&1 || true
          }
          trap cleanup EXIT

          sudo pg_dropcluster --stop "${PG_MAJOR}" tsdbtest >/dev/null 2>&1 || true
          sudo rm -rf "/etc/postgresql/${PG_MAJOR}/tsdbtest" "/var/lib/postgresql/${PG_MAJOR}/tsdbtest" "/var/log/postgresql/postgresql-${PG_MAJOR}-tsdbtest.log"
          sudo pg_createcluster "${PG_MAJOR}" tsdbtest -p "${TEST_PORT}" --start >/tmp/timescaledb-cluster.log 2>&1
          echo "shared_preload_libraries = 'timescaledb'" | sudo tee "/etc/postgresql/${PG_MAJOR}/tsdbtest/conf.d/timescaledb.conf" >/dev/null
          sudo systemctl restart "postgresql@${PG_MAJOR}-tsdbtest" >/tmp/timescaledb-restart.log 2>&1

          sudo -u postgres psql -p "${TEST_PORT}" -d postgres -c "CREATE EXTENSION IF NOT EXISTS timescaledb;" >/tmp/timescaledb-create-extension.txt
          sudo -u postgres psql -p "${TEST_PORT}" -d postgres -c "SELECT extversion FROM pg_extension WHERE extname = 'timescaledb';" >/tmp/timescaledb-extversion.txt
          sudo -u postgres psql -p "${TEST_PORT}" -d postgres -c "CREATE TABLE IF NOT EXISTS metrics(time timestamptz NOT NULL, value double precision);" >/tmp/timescaledb-create-table.txt
          sudo -u postgres psql -p "${TEST_PORT}" -d postgres -c "SELECT create_hypertable('metrics', 'time', if_not_exists => TRUE);" >/tmp/timescaledb-hypertable.txt

          if grep -q "${TIMESCALEDB_VERSION}" /tmp/timescaledb-extversion.txt && grep -q "metrics" /tmp/timescaledb-hypertable.txt; then
            echo "status=passed" >> "$GITHUB_OUTPUT"
          else
            cat /tmp/timescaledb-cluster.log || true
            cat /tmp/timescaledb-restart.log || true
            cat /tmp/timescaledb-extversion.txt || true
            cat /tmp/timescaledb-hypertable.txt || true
            echo "status=failed" >> "$GITHUB_OUTPUT"
            exit 1
          fi

          END_TIME=$(date +%s)
          echo "duration=$((END_TIME - START_TIME))" >> "$GITHUB_OUTPUT"

      - name: Test 6 - Regression Validation
        id: test6
        continue-on-error: true
        run: |
          set -euo pipefail
          START_TIME=$(date +%s)
          CURRENT="${{ steps.version.outputs.version || env.TIMESCALEDB_VERSION }}"
          LATEST="${{ steps.version.outputs.latest }}"
          echo "current_version=${CURRENT}" >> "$GITHUB_OUTPUT"
          echo "latest_version=${LATEST:-lookup_failed}" >> "$GITHUB_OUTPUT"
          echo "=== Regression Validation (Next Version Install) ==="
          echo "Current version tested in Tests 1-5: ${CURRENT}"

          if [ -z "${LATEST}" ]; then
            echo "next_installed_version=lookup_failed" >> "$GITHUB_OUTPUT"
            echo "decision=next_lookup_failed" >> "$GITHUB_OUTPUT"
            echo "regression_result=Next version install failed on Arm64" >> "$GITHUB_OUTPUT"
            echo "comparison=Current pinned TimescaleDB ${CURRENT} passed smoke tests in this run, but the next stable TimescaleDB release could not be resolved automatically." >> "$GITHUB_OUTPUT"
            echo "Next version targeted for install validation: lookup_failed"
            echo "Next version installed on Arm64: lookup_failed"
            echo "Regression install result: Next version install failed on Arm64"
            echo "Details: Current pinned TimescaleDB ${CURRENT} passed smoke tests in this run, but the next stable TimescaleDB release could not be resolved automatically."
            echo "status=failed" >> "$GITHUB_OUTPUT"
            END_TIME=$(date +%s)
            echo "duration=$((END_TIME - START_TIME))" >> "$GITHUB_OUTPUT"
            exit 1
          fi

          echo "Next version targeted for install validation: ${LATEST}"

          if [ "${CURRENT}" = "${LATEST}" ]; then
            echo "next_installed_version=${CURRENT}" >> "$GITHUB_OUTPUT"
            echo "decision=metadata_review_required" >> "$GITHUB_OUTPUT"
            echo "regression_result=Current stable TimescaleDB version already validated on Arm64" >> "$GITHUB_OUTPUT"
            echo "comparison=Pinned baseline already matches the latest stable upstream release. Repin Tests 1-5 to an older dashboard-aligned baseline so Test 6 validates a newer Arm64 release." >> "$GITHUB_OUTPUT"
            echo "Next version installed on Arm64: ${CURRENT}"
            echo "Regression install result: Current stable TimescaleDB version already validated on Arm64"
            echo "Details: Current pinned TimescaleDB ${CURRENT} was smoke-tested in this run, including package metadata, extension creation, and hypertable creation on PostgreSQL ${PG_MAJOR}. The validated pinned package already matches the latest stable TimescaleDB release."
            echo "status=failed" >> "$GITHUB_OUTPUT"
            END_TIME=$(date +%s)
            echo "duration=$((END_TIME - START_TIME))" >> "$GITHUB_OUTPUT"
            exit 1
          fi

          CANDIDATE_PACKAGE=$(apt-cache madison "postgresql-${PG_MAJOR}-timescaledb" | awk -v want="${LATEST}" '$3 ~ "^"want { print $3; exit }')
          if [ -z "${CANDIDATE_PACKAGE}" ]; then
            echo "next_installed_version=package_unavailable" >> "$GITHUB_OUTPUT"
            echo "decision=hold_current_arm_dependency_gap" >> "$GITHUB_OUTPUT"
            echo "regression_result=Next version install failed on Arm64" >> "$GITHUB_OUTPUT"
            echo "comparison=Current pinned TimescaleDB ${CURRENT} passed smoke tests in this run, but the candidate ${LATEST} package is not currently available in the configured Arm64 apt repository for PostgreSQL ${PG_MAJOR}." >> "$GITHUB_OUTPUT"
            echo "Next version installed on Arm64: package_unavailable"
            echo "Regression install result: Next version install failed on Arm64"
            echo "Details: Current pinned TimescaleDB ${CURRENT} passed smoke tests in this run, but the candidate ${LATEST} package is not currently available in the configured Arm64 apt repository for PostgreSQL ${PG_MAJOR}."
            echo "status=failed" >> "$GITHUB_OUTPUT"
            END_TIME=$(date +%s)
            echo "duration=$((END_TIME - START_TIME))" >> "$GITHUB_OUTPUT"
            exit 1
          fi

          export DEBIAN_FRONTEND=noninteractive
          sudo apt-get install -y --allow-downgrades "postgresql-${PG_MAJOR}-timescaledb=${CANDIDATE_PACKAGE}" >/tmp/timescaledb-candidate-install.log 2>&1
          NEXT_INSTALLED=$(dpkg-query -W -f='${Version}\n' "postgresql-${PG_MAJOR}-timescaledb" | grep -Eo '[0-9]+\.[0-9]+\.[0-9]+' | head -1 || true)
          REG_PORT=$(python3 - <<'PY'
          import socket
          with socket.socket() as s:
              s.bind(("", 0))
              print(s.getsockname()[1])
          PY
          )

          cleanup_candidate() {
            sudo pg_dropcluster --stop "${PG_MAJOR}" tsdbreg >/dev/null 2>&1 || true
          }
          trap cleanup_candidate EXIT

          sudo pg_dropcluster --stop "${PG_MAJOR}" tsdbreg >/dev/null 2>&1 || true
          sudo rm -rf "/etc/postgresql/${PG_MAJOR}/tsdbreg" "/var/lib/postgresql/${PG_MAJOR}/tsdbreg" "/var/log/postgresql/postgresql-${PG_MAJOR}-tsdbreg.log"
          sudo pg_createcluster "${PG_MAJOR}" tsdbreg -p "${REG_PORT}" --start >/tmp/timescaledb-reg-cluster.log 2>&1
          echo "shared_preload_libraries = 'timescaledb'" | sudo tee "/etc/postgresql/${PG_MAJOR}/tsdbreg/conf.d/timescaledb.conf" >/dev/null
          sudo systemctl restart "postgresql@${PG_MAJOR}-tsdbreg" >/tmp/timescaledb-reg-restart.log 2>&1
          sudo -u postgres psql -p "${REG_PORT}" -d postgres -c "CREATE EXTENSION IF NOT EXISTS timescaledb;" >/tmp/timescaledb-reg-create-extension.txt
          sudo -u postgres psql -p "${REG_PORT}" -d postgres -c "SELECT extversion FROM pg_extension WHERE extname = 'timescaledb';" >/tmp/timescaledb-reg-extversion.txt
          sudo -u postgres psql -p "${REG_PORT}" -d postgres -c "CREATE TABLE IF NOT EXISTS metrics(time timestamptz NOT NULL, value double precision);" >/tmp/timescaledb-reg-create-table.txt
          sudo -u postgres psql -p "${REG_PORT}" -d postgres -c "SELECT create_hypertable('metrics', 'time', if_not_exists => TRUE);" >/tmp/timescaledb-reg-hypertable.txt

          if [ "${NEXT_INSTALLED}" = "${LATEST}" ] && \
             grep -q "${LATEST}" /tmp/timescaledb-reg-extversion.txt && \
             grep -q "metrics" /tmp/timescaledb-reg-hypertable.txt; then
            echo "next_installed_version=${NEXT_INSTALLED}" >> "$GITHUB_OUTPUT"
            echo "decision=next_install_validated" >> "$GITHUB_OUTPUT"
            echo "regression_result=Next version installed successfully on Arm64" >> "$GITHUB_OUTPUT"
            echo "comparison=Current pinned TimescaleDB ${CURRENT} passed smoke tests in this run. Regression validation installed candidate ${LATEST} from the Arm64 apt repository, created the extension, and created the hypertable successfully on PostgreSQL ${PG_MAJOR}." >> "$GITHUB_OUTPUT"
            echo "Next version installed on Arm64: ${NEXT_INSTALLED}"
            echo "Regression install result: Next version installed successfully on Arm64"
            echo "Details: Current pinned TimescaleDB ${CURRENT} passed smoke tests in this run. Regression validation installed candidate ${LATEST} from the Arm64 apt repository, created the extension, and created the hypertable successfully on PostgreSQL ${PG_MAJOR}."
            echo "status=passed" >> "$GITHUB_OUTPUT"
          else
            cat /tmp/timescaledb-candidate-install.log || true
            cat /tmp/timescaledb-reg-cluster.log || true
            cat /tmp/timescaledb-reg-restart.log || true
            cat /tmp/timescaledb-reg-extversion.txt || true
            cat /tmp/timescaledb-reg-hypertable.txt || true
            echo "next_installed_version=${NEXT_INSTALLED:-install_failed}" >> "$GITHUB_OUTPUT"
            echo "decision=next_install_failed" >> "$GITHUB_OUTPUT"
            echo "regression_result=Next version install failed on Arm64" >> "$GITHUB_OUTPUT"
            echo "comparison=Current pinned TimescaleDB ${CURRENT} passed smoke tests in this run, but candidate ${LATEST} did not complete the expected Arm64 package install and extension validation path." >> "$GITHUB_OUTPUT"
            echo "Next version installed on Arm64: ${NEXT_INSTALLED:-install_failed}"
            echo "Regression install result: Next version install failed on Arm64"
            echo "Details: Current pinned TimescaleDB ${CURRENT} passed smoke tests in this run, but candidate ${LATEST} did not complete the expected Arm64 package install and extension validation path."
            echo "status=failed" >> "$GITHUB_OUTPUT"
            END_TIME=$(date +%s)
            echo "duration=$((END_TIME - START_TIME))" >> "$GITHUB_OUTPUT"
            exit 1
          fi

          END_TIME=$(date +%s)
          echo "duration=$((END_TIME - START_TIME))" >> "$GITHUB_OUTPUT"

      - name: Show regression validation details
        if: always()
        run: |
          DECISION="${{ steps.test6.outputs.decision || 'manual_review_needed' }}"
          REGRESSION_RESULT="${{ steps.test6.outputs.regression_result || '' }}"
          if [ -z "$REGRESSION_RESULT" ]; then
            case "$DECISION" in
              next_install_validated|safe_to_upgrade)
                REGRESSION_RESULT="Next version installed successfully on Arm64"
                ;;
              next_install_failed|next_lookup_failed)
                REGRESSION_RESULT="Next version install failed on Arm64"
                ;;
              metadata_review_required|current_is_latest_stable|pinned_is_latest|already_current|already_at_latest|at_latest|current_is_latest|current_is_latest_validated_release|current_is_latest_validated_3_2_x|pinned_latest_eol)
                REGRESSION_RESULT="Tests 1-5 baseline must be older than the latest stable release"
                ;;
              hold_current|hold_current_until_regression_run|hold_current_arm_dependency_gap|metadata_review_required|upgrade_candidate_available|manual_review_needed|manual_review_needed_for_product_upgrade|keep_current)
                REGRESSION_RESULT="Pending regression install validation"
                ;;
              *)
                if [ "${{ steps.test6.outputs.status || 'skipped' }}" = "passed" ]; then
                  REGRESSION_RESULT="Regression validation passed"
                else
                  REGRESSION_RESULT="Regression validation failed"
                fi
                ;;
            esac
          fi

          echo "=== Regression Validation (Next Version Install) ==="
          echo "Current version tested in Tests 1-5: ${{ steps.test6.outputs.current_version || steps.version.outputs.version || steps.metadata.outputs.current_version || 'unknown' }}"
          echo "Next version targeted for install validation: ${{ steps.test6.outputs.latest_version || steps.metadata.outputs.latest_version || 'unknown' }}"
          echo "Next version installed on Arm64: ${{ steps.test6.outputs.next_installed_version || 'not_installed' }}"
          echo "Regression install result: ${REGRESSION_RESULT}"
          echo "Details: ${{ steps.test6.outputs.comparison || steps.test6.outputs.note || 'Regression comparison summary unavailable.' }}"

      - name: Calculate test summary
        if: always()
        id: summary
        run: |
          PASSED=0
          FAILED=0
          CORE_FAILED=0
          TOTAL_DURATION=0

          check_core_test() {
            local status="$1"
            local duration="$2"
            if [ "$status" = "passed" ]; then
              PASSED=$((PASSED + 1))
            else
              FAILED=$((FAILED + 1))
              CORE_FAILED=$((CORE_FAILED + 1))
            fi
            TOTAL_DURATION=$((TOTAL_DURATION + duration))
          }

          check_core_test "${{ steps.test1.outputs.status || 'failed' }}" "${{ steps.test1.outputs.duration || 0 }}"
          check_core_test "${{ steps.test2.outputs.status || 'failed' }}" "${{ steps.test2.outputs.duration || 0 }}"
          check_core_test "${{ steps.test3.outputs.status || 'failed' }}" "${{ steps.test3.outputs.duration || 0 }}"
          check_core_test "${{ steps.test4.outputs.status || 'failed' }}" "${{ steps.test4.outputs.duration || 0 }}"
          check_core_test "${{ steps.test5.outputs.status || 'failed' }}" "${{ steps.test5.outputs.duration || 0 }}"

          if [ "${{ steps.test6.outputs.status || 'failed' }}" = "passed" ]; then
            PASSED=$((PASSED + 1))
          else
            FAILED=$((FAILED + 1))
          fi
          TOTAL_DURATION=$((TOTAL_DURATION + ${{ steps.test6.outputs.duration || 0 }}))

          echo "passed=$PASSED" >> "$GITHUB_OUTPUT"
          echo "failed=$FAILED" >> "$GITHUB_OUTPUT"
          echo "core_failed=$CORE_FAILED" >> "$GITHUB_OUTPUT"
          echo "duration=$TOTAL_DURATION" >> "$GITHUB_OUTPUT"

          if [ "$CORE_FAILED" -eq 0 ]; then
            echo "badge_status=passing" >> "$GITHUB_OUTPUT"
          else
            echo "badge_status=failing" >> "$GITHUB_OUTPUT"
          fi

          if [ "$CORE_FAILED" -eq 0 ] && [ "${{ steps.test6.outputs.status || 'failed' }}" = "passed" ]; then
            echo "overall_status=success" >> "$GITHUB_OUTPUT"
          else
            echo "overall_status=failure" >> "$GITHUB_OUTPUT"
            exit 1
          fi

      - name: Create test summary
        if: always()
        run: |
          echo "## TimescaleDB Test Results" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "- **Version:** ${{ steps.version.outputs.version }}" >> "$GITHUB_STEP_SUMMARY"
          echo "- **Status:** ${{ steps.summary.outputs.overall_status }}" >> "$GITHUB_STEP_SUMMARY"
          echo "- **Tests Passed:** ${{ steps.summary.outputs.passed }}" >> "$GITHUB_STEP_SUMMARY"
          echo "- **Tests Failed:** ${{ steps.summary.outputs.failed }}" >> "$GITHUB_STEP_SUMMARY"
          echo "- **Duration:** ${{ steps.summary.outputs.duration || 0 }}s" >> "$GITHUB_STEP_SUMMARY"
          echo "- **Runner:** ubuntu-24.04 (arm64)" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "### Test Details" >> "$GITHUB_STEP_SUMMARY"
          echo "1. Check extension assets exist: ${{ steps.test1.outputs.status || 'skipped' }}" >> "$GITHUB_STEP_SUMMARY"
          echo "2. Check package version: ${{ steps.test2.outputs.status || 'skipped' }}" >> "$GITHUB_STEP_SUMMARY"
          echo "3. Check control metadata: ${{ steps.test3.outputs.status || 'skipped' }}" >> "$GITHUB_STEP_SUMMARY"
          echo "4. Architecture Verification: ${{ steps.test4.outputs.status || 'skipped' }}" >> "$GITHUB_STEP_SUMMARY"
          echo "5. Functional Validation: ${{ steps.test5.outputs.status || 'skipped' }}" >> "$GITHUB_STEP_SUMMARY"
          echo "6. Regression Validation: ${{ steps.test6.outputs.status || 'skipped' }}" >> "$GITHUB_STEP_SUMMARY"
          echo "" >> "$GITHUB_STEP_SUMMARY"
          echo "### Regression Validation (Next Version Install)" >> "$GITHUB_STEP_SUMMARY"
          echo "- **Current version tested in Tests 1-5:** ${{ steps.test6.outputs.current_version || steps.version.outputs.version || 'unknown' }}" >> "$GITHUB_STEP_SUMMARY"
          echo "- **Next version targeted for install validation:** ${{ steps.test6.outputs.latest_version || 'unknown' }}" >> "$GITHUB_STEP_SUMMARY"
          echo "- **Next version installed on Arm64:** ${{ steps.test6.outputs.next_installed_version || 'not_installed' }}" >> "$GITHUB_STEP_SUMMARY"
          echo "- **Regression install result:** ${{ steps.test6.outputs.regression_result || 'Regression result unavailable.' }}" >> "$GITHUB_STEP_SUMMARY"
          echo "${{ steps.test6.outputs.comparison || steps.test6.outputs.note || 'Pinned current version passed smoke tests.' }}" >> "$GITHUB_STEP_SUMMARY"
