name: Test Platform Mesh Operator on Arm64

on:
  workflow_dispatch:
  workflow_call:
    outputs:
      contract_version: { value: "${{ jobs.test-platform-mesh-operator.outputs.contract_version }}" }
      package_slug: { value: "${{ jobs.test-platform-mesh-operator.outputs.package_slug }}" }
      package_name: { value: "${{ jobs.test-platform-mesh-operator.outputs.package_name }}" }
      package_version: { value: "${{ jobs.test-platform-mesh-operator.outputs.package_version }}" }
      run_status: { value: "${{ jobs.test-platform-mesh-operator.outputs.run_status }}" }
      badge_status: { value: "${{ jobs.test-platform-mesh-operator.outputs.badge_status }}" }
      core_failed: { value: "${{ jobs.test-platform-mesh-operator.outputs.core_failed }}" }
      tests_passed: { value: "${{ jobs.test-platform-mesh-operator.outputs.tests_passed }}" }
      tests_failed: { value: "${{ jobs.test-platform-mesh-operator.outputs.tests_failed }}" }
      tests_skipped: { value: "${{ jobs.test-platform-mesh-operator.outputs.tests_skipped }}" }
      duration_seconds: { value: "${{ jobs.test-platform-mesh-operator.outputs.duration_seconds }}" }
      regression_status: { value: "${{ jobs.test-platform-mesh-operator.outputs.regression_status }}" }
      regression_decision: { value: "${{ jobs.test-platform-mesh-operator.outputs.regression_decision }}" }
      regression_result: { value: "${{ jobs.test-platform-mesh-operator.outputs.regression_result }}" }
      regression_comparison: { value: "${{ jobs.test-platform-mesh-operator.outputs.regression_comparison }}" }
      regression_current_version: { value: "${{ jobs.test-platform-mesh-operator.outputs.regression_current_version }}" }
      regression_latest_version: { value: "${{ jobs.test-platform-mesh-operator.outputs.regression_latest_version }}" }
      regression_next_installed_version: { value: "${{ jobs.test-platform-mesh-operator.outputs.regression_next_installed_version }}" }
      regression_policy: { value: "${{ jobs.test-platform-mesh-operator.outputs.regression_policy }}" }
      run_id: { value: "${{ jobs.test-platform-mesh-operator.outputs.run_id }}" }
      run_attempt: { value: "${{ jobs.test-platform-mesh-operator.outputs.run_attempt }}" }
      job_name: { value: "${{ jobs.test-platform-mesh-operator.outputs.job_name }}" }
      dashboard_link: { value: "${{ jobs.test-platform-mesh-operator.outputs.dashboard_link }}" }
      timestamp: { value: "${{ jobs.test-platform-mesh-operator.outputs.timestamp }}" }

jobs:
  test-platform-mesh-operator:
    permissions:
      contents: read
      packages: read
    runs-on: ubuntu-24.04-arm
    outputs:
      contract_version: "2.0"
      package_slug: platform-mesh-operator
      package_name: Platform Mesh Operator
      package_version: ${{ steps.smoke.outputs.version || 'unknown' }}
      run_status: ${{ steps.summary.outputs.overall_status || 'failure' }}
      badge_status: ${{ steps.summary.outputs.badge_status || 'failing' }}
      core_failed: ${{ steps.summary.outputs.core_failed || '0' }}
      tests_passed: ${{ steps.summary.outputs.passed || '0' }}
      tests_failed: ${{ steps.summary.outputs.failed || '0' }}
      tests_skipped: ${{ steps.summary.outputs.skipped || '0' }}
      duration_seconds: ${{ steps.summary.outputs.duration || '0' }}
      regression_status: ${{ steps.smoke.outputs.test6_status || 'skipped' }}
      regression_decision: ${{ steps.smoke.outputs.regression_decision || 'not_configured' }}
      regression_result: ${{ steps.smoke.outputs.regression_result || '' }}
      regression_comparison: ${{ steps.smoke.outputs.regression_details || 'Regression comparison summary unavailable.' }}
      regression_current_version: ${{ steps.smoke.outputs.regression_current_version || steps.smoke.outputs.version || 'unknown' }}
      regression_latest_version: ${{ steps.smoke.outputs.regression_latest_version || 'unknown' }}
      regression_next_installed_version: ${{ steps.smoke.outputs.regression_next_installed_version || 'not_installed' }}
      regression_policy: applicable
      run_id: ${{ github.run_id }}
      run_attempt: ${{ github.run_attempt }}
      job_name: ${{ github.job }}
      dashboard_link: /opensource_packages/platform-mesh-operator
      timestamp: ${{ steps.metadata.outputs.timestamp }}
    steps:
      - uses: actions/checkout@v4
      - id: metadata
        run: echo "timestamp=$(date -u +%Y-%m-%dT%H:%M:%SZ)" >> "$GITHUB_OUTPUT"
      - id: smoke
        uses: ./.github/actions/run-missing-package-smoke
        env:
          GITHUB_TOKEN: ${{ github.token }}
        with:
          package_slug: platform-mesh-operator
          package_name: Platform Mesh Operator
          kind: source-image
          baseline_version: 0.1.0
          candidate_version: 0.72.14
          source_repo: platform-mesh/platform-mesh-operator
          source_tag: v0.1.0
          expected_paths: go.mod,Dockerfile,PROJECT
          baseline_image: ghcr.io/platform-mesh/platform-mesh-operator:v0.1.0
          candidate_image: ghcr.io/platform-mesh/platform-mesh-operator:v0.72.14
      - id: summary
        if: always()
        uses: ./.github/actions/emit-package-result
        with:
          package_slug: platform-mesh-operator
          package_name: Platform Mesh Operator
          version: ${{ steps.smoke.outputs.version || 'unknown' }}
          install_status: ${{ steps.smoke.outputs.install_status || 'failed' }}
          test1_label: "Download baseline source tag"
          test2_label: "Verify expected source layout"
          test3_label: "Validate Arm64 image manifest"
          test4_label: "Pull baseline Arm64 image"
          test5_label: "Inspect baseline image on Arm64"
          test1_status: ${{ steps.smoke.outputs.test1_status || 'skipped' }}
          test2_status: ${{ steps.smoke.outputs.test2_status || 'skipped' }}
          test3_status: ${{ steps.smoke.outputs.test3_status || 'skipped' }}
          test4_status: ${{ steps.smoke.outputs.test4_status || 'skipped' }}
          test5_status: ${{ steps.smoke.outputs.test5_status || 'skipped' }}
          test6_status: ${{ steps.smoke.outputs.test6_status || 'skipped' }}
          test1_duration: ${{ steps.smoke.outputs.test1_duration || 0 }}
          test2_duration: ${{ steps.smoke.outputs.test2_duration || 0 }}
          test3_duration: ${{ steps.smoke.outputs.test3_duration || 0 }}
          test4_duration: ${{ steps.smoke.outputs.test4_duration || 0 }}
          test5_duration: ${{ steps.smoke.outputs.test5_duration || 0 }}
          test6_duration: ${{ steps.smoke.outputs.test6_duration || 0 }}
          regression_decision: ${{ steps.smoke.outputs.regression_decision || 'not_configured' }}
          regression_current_version: ${{ steps.smoke.outputs.regression_current_version || steps.smoke.outputs.version || 'unknown' }}
          regression_latest_version: ${{ steps.smoke.outputs.regression_latest_version || 'unknown' }}
          regression_next_installed_version: ${{ steps.smoke.outputs.regression_next_installed_version || 'not_installed' }}
          regression_result: ${{ steps.smoke.outputs.regression_result || 'Regression result unavailable.' }}
          regression_details: ${{ steps.smoke.outputs.regression_details || 'Regression comparison summary unavailable.' }}
      - if: always()
        run: |
          if [ "${{ steps.summary.outputs.should_fail }}" = "1" ]; then
            exit 1
          fi
