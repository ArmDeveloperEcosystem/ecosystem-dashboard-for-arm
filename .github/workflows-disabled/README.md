These workflows are intentionally archived outside `.github/workflows` so GitHub Actions will not discover or run them.

Current offboarded packages:
- `curve`
- `kettle`
- `mesos`

Reason:
- package pages are marked `works_on_arm: false`
- they are removed from active batch orchestration
- workflow files are preserved here for future reference/debugging if needed
